<% if (post.categories && post.categories.length){ %>
  <div class="article-category"><span class="czs-category-l"></span>
    <%- list_categories(post.categories, {
      show_count: false,
      class: 'article-category',
      style: 'none',
      separator: ' / '
    }) %>
  </div>
<% } %>