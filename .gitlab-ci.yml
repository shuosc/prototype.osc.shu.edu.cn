stages:
- deploy
deploy:
  stage: deploy
  script:
    - echo $(whoami)
    - npm install
    - npm install -g hexo-cli
    - hexo generate
    - cp public/* /var/www/osc.shu.edu.cn -R

  only:
    - master
  tags:
    - shell