stages:
- deploy
deploy:
  stage: deploy
  script:
    - cd osc.shu.edu.cn && hexo clean
    - git pull
    - npm install
    - hexo generate
    - cp public/* /var/www/osc.shu.edu.cn -R

  only:
    - master
  tags:
    - shell